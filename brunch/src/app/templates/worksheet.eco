<div id="worksheet-view">
  <table>
    <% if @rows: %>
    <% for row in @rows: %>
    <tr>
      <% for cell,index in row: %>
      <td id="<%= @rows.indexOf(row) + 1 %>-<%= index + 1 %>"  data-value="<%= cell %>"><%= cell %></td>
      <% end %>
    </tr>
    <% end %>
  </table>
</div>

